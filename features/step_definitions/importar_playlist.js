const assert = require('assert');
const { Given, When, Then } = require('@cucumber/cucumber');

Given('que el usuario ha abierto la aplicación MelodyMinds', function () {
    return 'pending';
});

When('el usuario selecciona la opción para importar playlists', function () {
    return 'pending';
});

Then('el usuario recibe una notificación confirmando que la playlist se ha importado correctamente', function () {
    return 'pending';
});

Given('que el usuario ha iniciado sesión en su cuenta de Spotify', function () {
    return 'pending';
});

When('el usuario elige importar una playlist desde Spotify', function () {
    return 'pending';
});

Then('el usuario recibe una notificación confirmando que la playlist se ha importado correctamente desde Spotify', function () {
    return 'pending';
});

Given('que el usuario ha iniciado sesión en su cuenta de YouTube Music', function () {
    return 'pending';
});

When('el usuario elige importar una playlist desde YouTube Music', function () {
    return 'pending';
});

Then('el usuario recibe una notificación confirmando que la playlist se ha importado correctamente desde YouTube Music', function () {
    return 'pending';
});

Given('que el usuario ha iniciado sesión en su cuenta de Apple Music', function () {
    return 'pending';
});

When('el usuario elige importar una playlist desde Apple Music', function () {
    return 'pending';
});

Then('el usuario recibe una notificación confirmando que la playlist se ha importado correctamente desde Apple Music', function () {
    return 'pending';
});
