const assert = require('assert');
const { Given, When, Then } = require('@cucumber/cucumber');

Given('el usuario está registrandose', function () {
    // Código para el paso Given
    return 'pending';
});

When('el usuario presiona el boton de google', function () {
    // Código para el paso When
    return 'pending';
});

Then('al usuario se le manda al launcher de google', function () {
    // Código para el paso Then
    return 'pending';
});

Given('el usuario se encuentra en su perfil', function () {
    // Código para el paso Given
    return 'pending';
});

When('el usuario presiona el simbolo de vincular', function () {
    // Código para el paso When
    return 'pending';
});

Then('el usuario inicia sesión con google', function () {
    // Código para el paso Then
    return 'pending';
});

Given('el usuario está en la creación de cuenta', function () {
    // Código para el paso Given
    return 'pending';
});

When('el usuario termiana de usar el launcher de Google', function () {
    // Código para el paso When
    return 'pending';
});

Then('los criterios se llenan solos', function () {
    // Código para el paso Then
    return 'pending';
});

Given('el usuario se encuentra en la sección de preferencias de su perfil;', function () {
    // Código para el paso Given
    return 'pending';
});

When('el usuario presiona el botón de editar', function () {
    // Código para el paso When
    return 'pending';
});

Then('el usuario entra el modo editor de sus preferencias', function () {
    // Código para el paso Then
    return 'pending';
});

Given('el usuario sale de la creación de cuenta', function () {
    // Código para el paso Given
    return 'pending';
});

When('el usuario intenta jugaruna vez llenado todo', function () {
    // Código para el paso When
    return 'pending';
});

Then('el usuario puede jugar sin problemas', function () {
    // Código para el paso Then
    return 'pending';
});

Given('el usuario se encuentra en el inicio de sesión', function () {
    // Código para el paso Given
    return 'pending';
});

When('el usuario presiona el botón de iniciar sesión con google', function () {
    // Código para el paso When
    return 'pending';
});

Then('El usuario incia sesión desde el launcher de google', function () {
    // Código para el paso Then
    return 'pending';
});

Given('el usuario decidió iniciar sesión con google', function () {
    // Código para el paso Given
    return 'pending';
});

When('el usuario presiona el botón para iniciar sesión con google', function () {
    // Código para el paso When
    return 'pending';
});

Then('se abre el launcher para iniciar sesión', function () {
    // Código para el paso Then
    return 'pending';
});
