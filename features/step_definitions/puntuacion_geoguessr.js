const assert = require('assert');
const { Given, When, Then } = require('@cucumber/cucumber');

Given('el usuario está jugando cualquier categoría', function () {
    // Código para el paso Given
    return 'pending';
});

When('el usuario termina de jugar', function () {
    // Código para el paso When
    return 'pending';
});

Then('se le asigna una puntuación mejor mientras mejor lo hizo', function () {
    // Código para el paso Then
    return 'pending';
});

Given('El usuario termina de jugar', function () {
    // Código para el paso Given
    return 'pending';
});

When('el usuario haga una puntuacion perfecta', function () {
    // Código para el paso When
    return 'pending';
});

Then('el usuario recibe la cantidad maxima de puntos', function () {
    // Código para el paso Then
    return 'pending';
});

Given('el usuario inicia una partida', function () {
    // Código para el paso Given
    return 'pending';
});

When('aparece una pregunta', function () {
    // Código para el paso When
    return 'pending';
});

Then('la pregunta es adivinar una canción', function () {
    // Código para el paso Then
    return 'pending';
});

Given('el usuario acaba de contestar una pregunta', function () {
    // Código para el paso Given
    return 'pending';
});

When('el usuario contesta correctamente en los primeros {int} segundos', function (int) {
    // Código para el paso When
    return 'pending';
});

Then('se le asigna la puntuación maxima de la pregunta', function () {
    // Código para el paso Then
    return 'pending';
});

Given('el usuario está contestando una pregunta', function () {
    // Código para el paso Given
    return 'pending';
});

When('el usuario deja pasar el tiempo', function () {
    // Código para el paso When
    return 'pending';
});

Then('la puntuacion maxima posible va bajando', function () {
    // Código para el paso Then
    return 'pending';
});