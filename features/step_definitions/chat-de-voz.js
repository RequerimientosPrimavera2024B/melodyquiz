const { Given, When, Then } = require('@cucumber/cucumber');

Given('el usuario se encuentra en una actividad de juego compatible con chat de voz', function () {
    return 'pending';
});

When('el usuario selecciona la opción "Activar Chat de Voz"', function () {
    return 'pending';
});

Then('el sistema activa el chat de voz', function () {
    return 'pending';
});

Then('los usuarios pueden comenzar a comunicarse mediante voz', function () {
    return 'pending';
});

Given('el usuario se encuentra en una actividad de juego y selecciona "Activar Chat de Voz"', function () {
    return 'pending';
});

When('el sistema no detecta un micrófono conectado', function () {
    return 'pending';
});

Then('se muestra un mensaje de error al usuario', function () {
    return 'pending';
});

Then('se sugiere conectar un micrófono para usar el chat de voz', function () {
    return 'pending';
});

Given('el usuario se encuentra utilizando el chat de voz', function () {
    return 'pending';
});

When('hay una interrupción de la conexión de red', function () {
    return 'pending';
});

Then('se muestra un mensaje de error sobre problemas de red', function () {
    return 'pending';
});

Then('se aconseja verificar la conexión para continuar usando el chat de voz', function () {
    return 'pending';
});
