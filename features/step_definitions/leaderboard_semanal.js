const assert = require('assert');
const { Given, When, Then } = require('@cucumber/cucumber');

Given('el usuario está en la sección de leaderboards', function () {
    // Código para el paso Given
    return 'pending';
});

When('el usuario presiona una categoría', function () {
    // Código para el paso When
    return 'pending';
});

Then('se le muestra al usuario el leaderboard de dicha categoría', function () {
    // Código para el paso Then
    return 'pending';
});

Given('El usuario se encuentra en la pantalla de inicio', function () {
    // Código para el paso Given
    return 'pending';
});

When('el usuario presiona la pestaña de leaderboards', function () {
    // Código para el paso When
    return 'pending';
});

Then('el usuario es mandado a ver las posibles categorías de leaderboards', function () {
    // Código para el paso Then
    return 'pending';
});

Given('el usuario se encuentra en la configuración', function () {
    // Código para el paso Given
    return 'pending';
});

When('el usuario presiona le switch de mostrar país', function () {
    // Código para el paso When
    return 'pending';
});

Then('el usuario muestra\\/ya no muestra su pais', function () {
    // Write code here that turns the phrase above into concrete actions
    return 'pending';
  });

Given('el usuario está jugando una partida', function () {
    // Código para el paso Given
    return 'pending';
});

When('el usuario termina la partida', function () {
    // Código para el paso When
    return 'pending';
});

Then('su score se suma a su score semanal', function () {
    // Código para el paso Then
    return 'pending';
});

Given('el usuario se encuentra en un leaderboard', function () {
    // Código para el paso Given
    return 'pending';
});

When('el usuario presiona el botón para encontrarse', function () {
    // Código para el paso When
    return 'pending';
});

Then('el usuario es llevado a su posición en el leaderboard', function () {
    // Código para el paso Then
    return 'pending';
});

Given('el sistema se encuentra en el ciclo', function () {
    // Código para el paso Given
    return 'pending';
});

When('el sistema detecta que son las {int}:{int}', function (int, int2) {
    // Código para el paso When
    return 'pending';
});

Then('Todas las scores semanales de los usuarios vuelven a {int}', function (int) {
    // Código para el paso Then
    return 'pending';
});
